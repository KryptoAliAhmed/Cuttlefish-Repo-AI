# Dockerfiles for Off-Chain AI Services

## 🔬 predictive_oracle/Dockerfile
```dockerfile
FROM python:3.10-slim
WORKDIR /app
COPY ./main.py .
RUN pip install fastapi uvicorn
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

## 🧠 generative_strategy/Dockerfile
```dockerfile
FROM python:3.10-slim
WORKDIR /app
COPY ./main.py .
RUN pip install fastapi uvicorn
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

## 🌿 sustainability_scorer/Dockerfile
```dockerfile
FROM python:3.10-slim
WORKDIR /app
COPY ./main.py .
RUN pip install fastapi uvicorn
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

## 🧩 dispute_resolver/Dockerfile
```dockerfile
FROM python:3.10-slim
WORKDIR /app
COPY ./main.py .
RUN pip install fastapi uvicorn
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

✅ All ready for `docker-compose up`. Want `docker-compose.override.yml` or simple `curl` test scripts?
